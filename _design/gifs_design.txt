{
	"_id": "_design/gifs",
	"views":
	{
		"all": {
    		"map": "function(doc) { if (doc.Type == 'gifs')  emit(null, doc) }"
    	},
    	"topNew": {
    		"map": "function(doc) { if (doc.Type == 'gifs')  emit((doc.TimeAdded+doc.Uses*7500), doc) }"
    	},
    	"new": {
    		"map": "function(doc) { if (doc.Type == 'gifs')  emit((doc.TimeAdded), doc) }"
    	},
    	"top": {
    		"map": "function(doc) { if (doc.Type == 'gifs')  emit((doc.Uses), doc) }"
    	}
	}
}